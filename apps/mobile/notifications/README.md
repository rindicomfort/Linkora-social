# Mobile notifications

The mobile app requests push permissions on first launch after a wallet is connected and stores the Expo token locally. The token is optionally registered with the indexer via `/api/notifications/register` when an indexer URL is configured.
